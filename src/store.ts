import { create } from "zustand";

// interface LoggedState {
//   keyName: string | null;
//   key: string;
//   setLogged: (token: string) => void;
// }

// const useStore = create<LoggedState>((set) => ({
//   keyName: "KakaoLogin",
//   key: "",
//   setLogged: (token) => set({ key: token }),
// }));

// export default useStore;
